name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
    
jobs:
  my_build_job:
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v3
      
      
      
          
      -name: Run JS file for test
      run: node js/ script.js
        
